server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 单页应用回退：所有未命中静态资源的路径都交给 index.html。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
